```sql
    create database AnotaAi;

    use AnotaAi;

    create table notes (
        id int auto_increment,
        title varchar(25),
        description varchar(200),
        primary key(id)
    );
```
